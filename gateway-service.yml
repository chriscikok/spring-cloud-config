zuul:
  prefix: /api
  routes:
    account:
      path: /account/**
      sensitiveHeaders:
      serviceId: account-service
    customer:
      path: /customer/**
      sensitiveHeaders:
      serviceId: customer-service
